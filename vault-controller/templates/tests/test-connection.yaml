apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vault-controller.fullname" . }}-test-connection"
  labels:
{{ include "vault-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "vault-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
